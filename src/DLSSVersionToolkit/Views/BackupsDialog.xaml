<Window x:Class="DLSSVersionToolkit.BackupsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NGX Backups" Height="440" Width="620"
        MinHeight="260" MinWidth="460"
        WindowStartupLocation="CenterOwner" Background="#0E0E0E"
        ShowInTaskbar="False" ResizeMode="CanResize" Loaded="Window_Loaded"
        SizeToContent="Manual"
        FontFamily="Segoe UI" TextOptions.TextFormattingMode="Display">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="NGX Backups" FontSize="18" FontWeight="Bold" Foreground="#E8E8E8"/>
        <TextBlock Grid.Row="1" Text="Every sync saves a backup of your previous NGX DLLs before replacing them. If a game has trouble after an update, pick one and Restore." FontSize="12" Foreground="#9A9A9A" TextWrapping="Wrap" Margin="0,4,0,0"/>

        <DataGrid Grid.Row="3" x:Name="BackupGrid" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="#171717" Foreground="#E8E8E8" BorderThickness="1" BorderBrush="#333"
                  RowHeight="30" ColumnHeaderHeight="32" AlternatingRowBackground="#1A1A1A"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#111"/>
                    <Setter Property="Foreground" Value="#BBB"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#2A2A2A"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Created" Binding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontFamily" Value="Consolas"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Foreground" Value="#E8E8E8"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Files" Binding="{Binding FileCount}" Width="70"/>
                <DataGridTextColumn Header="Location" Binding="{Binding PathDisplay}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="RestoreButton" Content="Restore selected" Width="130" Margin="0,0,8,0"
                    Padding="8,6" Background="#76B900" Foreground="#0A0A0A" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="RestoreButton_Click"/>
            <Button x:Name="DoneButton" Content="Close" Width="90" Padding="8,6"
                    Background="#2A2A2A" Foreground="#DDD" BorderThickness="1" BorderBrush="#444"
                    Cursor="Hand" Click="DoneButton_Click"/>
        </StackPanel>
    </Grid>
</Window>